/**
 * Common test IDs used across components
 */
export const TEST_IDS = {
  SWITCH_THUMB: 'switchThumb',
  BUTTON: 'button',
  ICON: 'icon',
  TEXT: 'text',
  LOADING: 'loading',
  MODAL: 'modal',
  TOAST: 'toast',
} as const;

/**
 * Common animation durations
 */
export const ANIMATION_DURATIONS = {
  DEFAULT: 300,
  PRESS_DELAY: 1000,
} as const;

/**
 * Common roles used in testing
 */
export const ROLES = {
  BUTTON: 'button',
  SWITCH: 'switch',
  TEXT: 'text',
  GROUP: 'group',
  HEADING: 'heading',
  IMAGE: 'image',
} as const;
